# 📁 인천환경공단 하수도 모니터링 시스템 - 파일 구조 가이드

**작성일**: 2025-01-03
**프로젝트**: AI 기반 방류수 수질 예측 모니터링 시스템
**개발**: 홍익정보기술(주)

---

## 🎯 프로젝트 개요

**위치**: `D:\이정은\인천환경공단\wastewater-monitoring-system`
**타입**: React(프론트엔드) + FastAPI(백엔드)
**목적**: AI 기반 방류수 수질 예측 모니터링

---

## 📂 1. 루트 디렉토리 파일들

### 📄 설치/실행 배치 파일

| 파일명 | 용도 | 실행 시 동작 |
|--------|------|-------------|
| `0_INSTALL.bat` | 전체 설치 | 프론트엔드 + 백엔드 패키지 설치 |
| `START_ALL.bat` | 전체 실행 | 프론트엔드 개발서버 + 백엔드 서버 동시 실행 |
| `BUILD_AND_RUN.bat` | 빌드 후 실행 | 프론트엔드 빌드 → 프로덕션 실행 |
| `RUN_PRODUCTION.bat` | 프로덕션 실행 | 빌드된 파일로 서버 실행 |

### 📝 문서 파일

| 파일명 | 내용 | 대상 |
|--------|------|------|
| `README.md` | 프로젝트 전체 설명서 (226줄) | 개발자 |
| `README_SIMPLE.txt` | 간단 사용 설명서 | 일반 사용자 |
| `DB연동_작업_매뉴얼.md` | 데이터베이스 연동 가이드 (1,119줄) | 비개발자 |
| `DB연동_빠른시작.txt` | DB 연동 빠른 시작 | 개발자 |
| `API_쉬운_설명.txt` | API 테스트 방법 (238줄) | 비개발자 |

### 🗄️ 데이터베이스 스키마

| 파일명 | 데이터베이스 | 테이블 수 |
|--------|-------------|----------|
| `create_tables.sql` | PostgreSQL | 6개 테이블 |
| `create_tables_mysql.sql` | MySQL | 6개 테이블 |

**테이블 목록**:
- `sensor_data` - 센서 측정 데이터
- `process_status` - 처리장 공종 현황
- `tms_data` - 방류 TMS 데이터
- `predictions` - AI 예측 데이터
- `alarms` - 알림 이력
- `threshold_settings` - 임계값 설정

### ⚙️ 설정 파일

| 파일명 | 용도 |
|--------|------|
| `package.json` | 프론트엔드 패키지 정보 |
| `vite.config.js` | Vite 빌드 설정 |
| `index.html` | 메인 HTML (React 마운트) |

---

## 🎨 2. 프론트엔드 (src/) - React 애플리케이션

### 📌 2.1. 메인 진입점

| 파일 | 역할 |
|------|------|
| `src/main.jsx` | React 앱 시작점, DOM에 마운트 |
| `src/App.jsx` | 라우팅 설정, 페이지 연결 |

**App.jsx 라우팅 구조**:
```
/ → /monitoring (리다이렉트)
/monitoring → 모니터링 페이지
/history/data → 데이터 이력관리
/history/prediction → 수질예측 이력관리
/history/alarm → 알림 이력관리
/settings → 환경설정
```

---

### 🖼️ 2.2. 컴포넌트 (components/)

| 파일 | 역할 | 사용 위치 |
|------|------|----------|
| **Layout.jsx** | 전체 레이아웃 (사이드바 + 헤더 + 콘텐츠) | App.jsx |
| **Header.jsx** | 상단 헤더 (페이지 제목, 현재시각, 로고) | Layout.jsx |
| **Sidebar.jsx** | 왼쪽 사이드바 (메뉴 네비게이션) | Layout.jsx |
| **SearchFilter.jsx** | 검색 필터 UI (날짜, 드롭다운 등) | 모든 이력 페이지 |
| **DataTable.jsx** | 데이터 테이블 (페이징, 정렬) | 모든 이력 페이지 |

**컴포넌트 관계도**:
```
Layout
 ├─ Sidebar (메뉴)
 ├─ Header (상단바)
 └─ Pages (페이지 콘텐츠)
      ├─ SearchFilter (검색)
      └─ DataTable (테이블)
```

---

### 📄 2.3. 페이지 (pages/) - 각 화면

#### 📊 Monitoring.jsx - 실시간 모니터링 대시보드
**URL**: `/monitoring`
**파일**: `src/pages/Monitoring.jsx` (629줄)
**역할**: 메인 대시보드, 실시간 데이터 표시

**표시 내용**:
- 처리장 공종 현황 (유입량, 방류량)
- 5개 지(zone) 센서 데이터 테이블
  - 혐기조: ORP, pH
  - 무산소조: ORP, pH
  - 호기조: DO, pH, MLSS
- 방류 TMS 실시간 값 (TOC, SS, T-N, T-P)
- AI 예측 카드 (3시간 후 예측)
- 실시간 알림 피드

**주요 기능**:
- 5초마다 자동 새로고침
- 임계값 초과 시 빨간색 표시
- WebSocket 실시간 통신 (향후)

---

#### 📋 DataHistory.jsx - 데이터 이력관리
**URL**: `/history/data`
**파일**: `src/pages/DataHistory.jsx`
**역할**: 센서 측정 데이터 이력 조회

**검색 조건**:
- 조회 단위 (시간/1분)
- 지 (1지~5지)
- 날짜 범위

**표시 내용**:
- 측정 일시, 지
- 혐기조/무산소조/호기조 센서 값

**주요 기능**:
- API: `POST /api/history/sensor-data`
- Excel 다운로드
- 페이징 (15건/페이지)

---

#### 🔮 PredictionHistory.jsx - 수질예측 이력관리
**URL**: `/history/prediction`
**파일**: `src/pages/PredictionHistory.jsx` (264줄)
**역할**: AI 예측 이력 조회

**검색 조건**:
- 조회 단위 (시간/1분)
- ~~지~~ (삭제됨! - 28-41줄 주석 처리)
- 예측결과 (전체/정상/비정상)
- 날짜 범위

**표시 내용**:
- 예측일시
- 예측결과 (정상/비정상)
- TOC, SS, T-N, T-P 예측값

**주요 기능**:
- API: `POST /api/history/predictions`
- Excel 다운로드
- T-N > 18 시 빨간색 표시

**주요 변수 (handleSearch 함수 내)**:
- `apiParams` (162줄): API 요청 파라미터 생성
  - `zone` 파라미터 제거됨 (163줄 주석 처리)
- `searchParams` (173줄): 조회 조건 저장 (Excel 다운로드용)

---

#### 🚨 AlarmHistory.jsx - 알림 이력관리 (통합)
**URL**: `/history/alarm`
**파일**: `src/pages/AlarmHistory.jsx`
**역할**: 공종 + 예측 알림 이력 통합 조회

**검색 조건 (공종)**:
- 지 (1지~5지)
- 공종 (혐기조/무산소조/호기조)
- 센서 (ORP/pH/DO/MLSS) - **동적 변경!**
- 날짜 범위

**검색 조건 (예측)**:
- 항목 (TOC/SS/T-N/T-P)
- 날짜 범위

**표시 내용**:
- 알림 일시, 알림 결과 (정상/비정상)
- 센서 값 또는 예측 값
- 알림 내용

**주요 기능**:
- 공종/예측 탭 전환
- 공종 선택 시 센서 옵션 자동 변경
  - 혐기조/무산소조 → ORP, pH만 (31-36줄)
  - 호기조 → DO, pH, MLSS (37-44줄)
- API: `POST /api/history/alarms/process` 또는 `prediction`
- Excel 다운로드 (type 구분 필수!)

**핵심 함수**:
- `getSensorOptions()` (19-48줄): 공종별 센서 목록 동적 생성
- `handleFilterChange()` (306-324줄): 공종 변경 시 센서 초기화 (318-320줄)
- `handleSearch()` (326줄~): API 파라미터에 `type: 'process'` 또는 `'prediction'` 포함 (386, 396줄)

---

#### ⚙️ Settings.jsx - 환경설정
**URL**: `/settings`
**파일**: `src/pages/Settings.jsx`
**역할**: 임계값 설정

**설정 항목**:
- 공종 임계값
  - 혐기조: ORP, pH 범위
  - 무산소조: ORP, pH 범위
  - 호기조: DO, pH, MLSS 범위
- 방류 임계값
  - TOC, SS, T-N, T-P 상한값

**주요 기능**:
- 임계값 입력 (상한/하한)
- localStorage 저장
- API: `PUT /api/settings/thresholds`

---

#### 📝 구버전 파일 (사용 안 함)
- `AlarmHistoryProcess.jsx` - AlarmHistory.jsx로 통합됨
- `AlarmHistoryPrediction.jsx` - AlarmHistory.jsx로 통합됨

---

### 📐 2.4. Context (context/)

| 파일 | 역할 |
|------|------|
| **ThresholdContext.jsx** | 임계값 전역 상태 관리 |

**제공 기능**:
- `processThresholds` - 공종 임계값
- `effluentThresholds` - 방류 임계값
- `updateProcessThreshold()` - 공종 임계값 업데이트
- `updateEffluentThreshold()` - 방류 임계값 업데이트
- `getProcessStatus()` - 센서 값 정상/비정상 판단
- `getEffluentStatus()` - 방류 값 정상/비정상 판단

**사용 위치**:
- Monitoring.jsx - 실시간 판단
- Settings.jsx - 임계값 설정

---

### 🔧 2.5. Utilities (utils/)

#### api.js - API 호출 함수 모음 (321줄)
**파일**: `src/utils/api.js`

**주요 API 함수**:
```javascript
// 1. 실시간 모니터링
monitoringAPI.getProcessStatus()    // 처리장 공종 현황
monitoringAPI.getZoneData()         // 5개 지 센서 데이터
monitoringAPI.getTMS()              // 방류 TMS 데이터
monitoringAPI.getAlerts(limit)      // 실시간 알림

// 2. AI 예측
predictionAPI.getForecast()         // 3시간 후 예측
predictionAPI.getForecast1Hour()    // 1시간 후 예측

// 3. 이력 조회
historyAPI.getSensorData(params)         // 센서 데이터 이력
historyAPI.getPredictions(params)        // 예측 이력
historyAPI.getAlarmProcess(params)       // 공종 알림 이력
historyAPI.getAlarmPrediction(params)    // 예측 알림 이력

// 4. Excel 다운로드
exportAPI.exportSensorData(params)   // 센서 데이터 Excel
exportAPI.exportPredictions(params)  // 예측 이력 Excel
exportAPI.exportAlarms(params)       // 알림 이력 Excel (type 필수!)

// 5. 설정
settingsAPI.getThresholds()          // 임계값 조회
settingsAPI.updateThresholds(data)   // 임계값 저장
```

#### formatNumber.js - 숫자 포맷팅 유틸리티
**파일**: `src/utils/formatNumber.js`

---

### 🪝 2.6. Hooks (hooks/)

| 파일 | 역할 |
|------|------|
| **useWebSocket.js** | WebSocket 연결 관리 Custom Hook |

**기능**:
- WebSocket 연결/해제
- 자동 재연결
- 실시간 데이터 수신

---

### 🎨 2.7. 스타일 (styles/)

#### global.css - 전역 스타일
**파일**: `src/styles/global.css`

**CSS 변수**:
```css
--primary-color: #1e3a8a    /* 인천환경공단 브랜드 컬러 */
--normal-color: #10b981     /* 정상 (녹색) */
--warning-color: #f59e0b    /* 경고 (노란색) */
--alert-color: #f97316      /* 주의 (주황색) */
--danger-color: #ef4444     /* 위험 (빨간색) */
```

---

## 🖥️ 3. 백엔드 (backend/) - FastAPI 서버

### 📌 3.1. 메인 파일

| 파일 | 역할 |
|------|------|
| `run.py` | 서버 시작 스크립트 |
| `app/main.py` | FastAPI 앱 진입점, 라우터 등록 |
| `app/config.py` | 설정 (DB URL, CORS 등) |
| `test_api.py` | API 테스트 스크립트 |
| `start.bat` | 백엔드 시작 배치 파일 |

---

### 🛣️ 3.2. 라우터 (routers/) - API 엔드포인트

#### monitoring.py - 실시간 모니터링 API
**엔드포인트**:
```
GET /api/monitoring/process-status    - 처리장 공종 현황
GET /api/monitoring/zone-data         - 5개 지 센서 데이터
GET /api/monitoring/tms                - 방류 TMS 데이터
GET /api/monitoring/alerts?limit=10    - 실시간 알림
```

---

#### prediction.py - AI 예측 API
**엔드포인트**:
```
GET /api/prediction/forecast           - 3시간 후 예측
GET /api/prediction/forecast/1hour     - 1시간 후 예측
```

---

#### history.py - 이력 조회 API
**엔드포인트**:
```
POST /api/history/sensor-data          - 센서 데이터 이력
POST /api/history/predictions          - 예측 이력
POST /api/history/alarms/process       - 공종 알림 이력
POST /api/history/alarms/prediction    - 예측 알림 이력
```

**Request Body 예시**:
```json
{
  "zone": "all",
  "result": "all",
  "startDateTime": "2025-01-01T00:00:00Z",
  "endDateTime": "2025-01-31T23:59:59Z",
  "interval": "hour",
  "page": 1,
  "pageSize": 10000
}
```

---

#### export.py - Excel 다운로드 API
**엔드포인트**:
```
POST /api/export/sensor-data           - 센서 데이터 Excel
POST /api/export/predictions           - 예측 이력 Excel
POST /api/export/alarms                - 알림 이력 Excel (type 필수!)
```

**파일명 형식**:
- `sensor_data_YYYYMMDD_HHMMSS.xlsx`
- `predictions_YYYYMMDD_HHMMSS.xlsx`
- `alarms_process_YYYYMMDD_HHMMSS.xlsx`
- `alarms_prediction_YYYYMMDD_HHMMSS.xlsx`

**중요**: exportAlarms는 `type` 파라미터 필수! (108-113줄)

---

#### settings.py - 설정 API
**엔드포인트**:
```
GET /api/settings/thresholds           - 임계값 조회
PUT /api/settings/thresholds           - 임계값 저장
```

---

### 📦 3.3. 모델 (models/)

#### schemas.py - Pydantic 데이터 모델 (341줄)
**파일**: `backend/app/models/schemas.py`

**주요 스키마**:
- `ProcessData` - 공종 현황 데이터
- `ZoneData` - 센서 데이터
- `TMSResponse` - TMS 응답
- `PredictionResponse` - 예측 응답
- `SensorDataRequest` - 센서 이력 요청
- `PredictionHistoryRequest` - 예측 이력 요청 (167줄: zone 필드 필수!)
- `AlarmProcessRequest` - 공종 알림 요청
- `AlarmPredictionRequest` - 예측 알림 요청
- `ExportRequest` - Excel 다운로드 요청 (310줄: type 필드 Optional)
- `ThresholdSettings` - 임계값 설정

---

### 🎲 3.4. 서비스 (services/)

#### data_generator.py - Mock 데이터 생성기 (493줄)
**파일**: `backend/app/services/data_generator.py`

**현재 상태**: 실제 DB 대신 임의 데이터 생성

**주요 함수**:
- `generate_process_status()` - 공종 현황
- `generate_zone_data()` - 센서 데이터
- `generate_tms_data()` - TMS 데이터
- `generate_prediction_data(hours)` - 예측 데이터
- `generate_alerts()` - 알림 데이터
- `generate_historical_sensor_data()` - 센서 이력
- `generate_historical_predictions()` - 예측 이력
- `generate_historical_alarms_process()` - 공종 알림 이력
- `generate_historical_alarms_prediction()` - 예측 알림 이력

**특징**:
- 실제 범위 내 랜덤 값 생성
- 10-20% 확률로 임계값 위반 시뮬레이션
- 시계열 데이터 생성

---

### 🔌 3.5. WebSocket (websocket/)

#### connection.py - WebSocket 연결 관리
**파일**: `backend/app/websocket/connection.py`

**기능**:
- 여러 클라이언트 동시 연결
- 실시간 데이터 브로드캐스트
- 자동 재연결 지원

**상태**: 준비됨 (실시간 통신용)

---

## 🗺️ 4. 전체 데이터 흐름도

### 사용자 → 화면 → API → 데이터

```
┌──────────────────────────────────────────┐
│ 1. 사용자 브라우저                        │
│    http://localhost:5173                 │
└──────────────────────────────────────────┘
                ↓
┌──────────────────────────────────────────┐
│ 2. React 프론트엔드 (Port 5173)          │
│    ├─ Layout (Sidebar + Header)         │
│    ├─ Monitoring.jsx                    │
│    ├─ DataHistory.jsx                   │
│    ├─ PredictionHistory.jsx             │
│    ├─ AlarmHistory.jsx                  │
│    └─ Settings.jsx                      │
└──────────────────────────────────────────┘
                ↓ HTTP / WebSocket
┌──────────────────────────────────────────┐
│ 3. FastAPI 백엔드 (Port 8000)            │
│    ├─ /api/monitoring/*                 │
│    ├─ /api/prediction/*                 │
│    ├─ /api/history/*                    │
│    ├─ /api/export/*                     │
│    └─ /api/settings/*                   │
└──────────────────────────────────────────┘
                ↓
┌──────────────────────────────────────────┐
│ 4. 데이터 소스                            │
│    ├─ data_generator.py (현재: Mock)    │
│    └─ PostgreSQL/MySQL (향후)          │
└──────────────────────────────────────────┘
```

---

## 📊 5. 페이지별 파일 연결 맵

### 모니터링 페이지
```
Browser → Monitoring.jsx
            ↓
         ThresholdContext.jsx (임계값)
            ↓
         api.js → monitoringAPI
            ↓
         Backend: monitoring.py
            ↓
         data_generator.py
```

### 이력 페이지 (공통 패턴)
```
Browser → [이력 페이지].jsx
            ↓
         SearchFilter.jsx (필터 UI)
            ↓
         DataTable.jsx (테이블 UI)
            ↓
         api.js → historyAPI / exportAPI
            ↓
         Backend: history.py / export.py
            ↓
         data_generator.py
```

### 설정 페이지
```
Browser → Settings.jsx
            ↓
         ThresholdContext.jsx (상태 관리)
            ↓
         localStorage (프론트 저장)
            ↓
         api.js → settingsAPI
            ↓
         Backend: settings.py
```

---

## 🔍 6. 파일 찾기 빠른 참조

### "어떤 페이지를 수정하고 싶어요"

| 원하는 변경 | 수정 파일 | 라인 수 |
|-----------|----------|---------|
| 메인 대시보드 | `src/pages/Monitoring.jsx` | 629줄 |
| 센서 이력 화면 | `src/pages/DataHistory.jsx` | ~250줄 |
| 예측 이력 화면 | `src/pages/PredictionHistory.jsx` | 264줄 |
| 알림 이력 화면 | `src/pages/AlarmHistory.jsx` | ~520줄 |
| 임계값 설정 화면 | `src/pages/Settings.jsx` | ~300줄 |
| 사이드바 메뉴 | `src/components/Sidebar.jsx` | ~100줄 |
| 상단 헤더 | `src/components/Header.jsx` | ~70줄 |
| 검색 필터 | `src/components/SearchFilter.jsx` | ~150줄 |
| 테이블 | `src/components/DataTable.jsx` | ~200줄 |

### "API를 수정하고 싶어요"

| API 종류 | 백엔드 파일 |
|---------|-----------|
| 실시간 모니터링 | `backend/app/routers/monitoring.py` |
| AI 예측 | `backend/app/routers/prediction.py` |
| 이력 조회 | `backend/app/routers/history.py` |
| Excel 다운로드 | `backend/app/routers/export.py` |
| 임계값 설정 | `backend/app/routers/settings.py` |
| Mock 데이터 | `backend/app/services/data_generator.py` (493줄) |
| 데이터 모델 | `backend/app/models/schemas.py` (341줄) |

### "설정을 바꾸고 싶어요"

| 설정 항목 | 파일 |
|----------|------|
| 프론트엔드 패키지 | `package.json` |
| 빌드 설정 | `vite.config.js` |
| 백엔드 설정 | `backend/app/config.py` |
| DB 스키마 | `create_tables.sql` |
| 전역 스타일 | `src/styles/global.css` |

---

## 🎯 7. 주요 변수명 용어집

### 전역적으로 자주 사용되는 변수명

| 용어 | 의미 | 예시 파일 | 위치 |
|------|------|----------|------|
| `filterValues` | 사용자가 선택한 필터 값 (상태 변수) | 모든 이력 페이지 | 상단 useState |
| `apiParams` | API 요청 파라미터 (로컬 변수) | 이력 페이지 | handleSearch 함수 내부 |
| `searchParams` | 마지막 조회 조건 저장 (상태 변수) | 이력 페이지 | 상단 useState |
| `tableData` | 테이블 표시 데이터 (상태 변수) | 모든 이력 페이지 | 상단 useState |
| `handleSearch` | 조회 버튼 클릭 함수 | 모든 이력 페이지 | 함수 정의 |
| `handleExport` | Excel 다운로드 함수 | 모든 이력 페이지 | 함수 정의 |
| `handleFilterChange` | 필터 변경 함수 | 모든 이력 페이지 | 함수 정의 |
| `currentPage` | 현재 페이지 번호 (상태 변수) | 모든 이력 페이지 | 상단 useState |

### 변수명 혼동 주의!

**params vs apiParams vs searchParams**:
```javascript
// 1. filterValues (상태 변수)
const [filterValues, setFilterValues] = useState({})
// → 사용자가 선택한 필터 값 저장
// { result: 'normal', startDate: '2025-01-01' }

// 2. apiParams (로컬 변수 - handleSearch 함수 내)
const apiParams = {
  result: filterValues.result,
  startDateTime: '2025-01-01T00:00:00Z',
  ...
}
// → API 요청용으로 가공된 파라미터
// ISO 형식, 추가 필드 등

// 3. searchParams (상태 변수)
setSearchParams(apiParams)
// → apiParams를 저장 (Excel 다운로드용)
```

---

## 🚨 8. 자주 발생하는 혼동 사항

### 혼동 1: "params 함수가 어디 있어요?"
**답**: `params`는 함수가 아니라 **변수**(상수)입니다!
- `apiParams`는 `handleSearch` 함수 **안**에서만 존재하는 로컬 변수
- 파일마다 다른 이름일 수 있음 (params, apiParams, requestParams 등)

### 혼동 2: "zone 파라미터를 삭제해야 하나요?"
**답**: 상황에 따라 다릅니다.
- **PredictionHistory.jsx**: zone 이미 삭제됨 (163줄 주석 처리)
- **AlarmHistory.jsx**: zone 사용 중 (공종 필터)
- **DataHistory.jsx**: zone 사용 중 (지 필터)

### 혼동 3: "Excel 다운로드가 안 돼요"
**확인 사항**:
1. 조회를 먼저 했는지 확인 (`searchParams`가 null이면 에러)
2. AlarmHistory의 경우 `type` 파라미터 있는지 확인 (386, 396줄)
3. 백엔드 export.py에서 `type` 파라미터로 분기 (113줄)

### 혼동 4: "공종 선택했는데 센서가 안 바뀌어요"
**확인 사항**:
1. `getSensorOptions()` 함수 구현되어 있는지 (AlarmHistory.jsx:19-48줄)
2. `handleFilterChange`에서 공종 변경 시 센서 초기화하는지 (318-320줄)
3. filters 배열에서 `options: getSensorOptions()` 사용하는지 (102줄)

---

## 📌 9. 핵심 요약

### 프론트엔드 (React)
- **5개 주요 페이지**: Monitoring, DataHistory, PredictionHistory, AlarmHistory, Settings
- **재사용 컴포넌트**: Layout, Header, Sidebar, SearchFilter, DataTable
- **상태 관리**: ThresholdContext (임계값)
- **API 통신**: api.js (321줄)

### 백엔드 (FastAPI)
- **5개 라우터**: monitoring, prediction, history, export, settings
- **데이터 생성**: data_generator.py (493줄, Mock 데이터)
- **스키마**: schemas.py (341줄, 모든 데이터 모델)

### 파일 개수
- **프론트엔드**: 27개 파일 (jsx/js/css)
- **백엔드**: 17개 파일 (.py)
- **문서**: 7개 파일 (README, 매뉴얼 등)
- **총계**: 약 50개 주요 파일

---

## 🔧 10. 개발 워크플로우

### 새 기능 추가 시
1. 프론트엔드: `src/pages/` 또는 `src/components/`에 파일 생성
2. 라우팅: `src/App.jsx`에 경로 추가
3. API: `src/utils/api.js`에 함수 추가
4. 백엔드: `backend/app/routers/`에 엔드포인트 추가
5. 스키마: `backend/app/models/schemas.py`에 모델 추가

### 버그 수정 시
1. 브라우저 F12 → 에러 메시지 확인
2. Network 탭 → API 요청/응답 확인
3. Console 탭 → 로그 확인
4. 해당 파일 찾기 (이 가이드 참조)
5. 수정 후 테스트

### 스타일 변경 시
1. 전역: `src/styles/global.css`
2. 컴포넌트별: 해당 `.css` 파일 (예: `Monitoring.css`)

---

## 📞 11. 문의 및 지원

**개발사**: 홍익정보기술(주)
**이메일**: bestcho93@nate.com
**문서 버전**: v1.0 (2025-01-03)

---

## 📝 12. 변경 이력

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 2025-01-03 | v1.0 | 초안 작성 | Claude Code |

---

**끝.**
